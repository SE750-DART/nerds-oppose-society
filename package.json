{
  "name": "nerds-oppose-society",
  "version": "0.1.0",
  "scripts": {
    "test": "(cd backend && npm run test); (cd frontend && npm run test)",
    "lint": "(cd backend && npm run lint); (cd frontend && npm run lint)",
    "lint:fix": "(cd backend && npm run lint:fix); (cd frontend && npm run lint:fix)",
    "format": "prettier . --write",
    "prepare": "[ \"$NODE_ENV\" != production ] && husky install || echo \"Husky skipped due to production\"",
    "start": "cd backend && npm run start-prod",
    "build": "cd backend && npm install --include=dev && npm run build && cd ../frontend && npm install --include=dev && npm run build && cp -R build ../backend/build/src/ && cd ..",
    "dev": "concurrently --kill-others-on-fail \"npm run api\" \"npm run frontend\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SE750-DART/nerds-oppose-society.git"
  },
  "bugs": {
    "url": "https://github.com/SE750-DART/nerds-oppose-society/issues"
  },
  "homepage": "https://github.com/SE750-DART/nerds-oppose-society#readme",
  "devDependencies": {
    "eslint": "^7.25.0",
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4",
    "prettier": "^2.2.1"
  },
  "lint-staged": {
    "backend/**/*": [
      "eslint --fix",
      "prettier --write --ignore-unknown"
    ],
    "frontend/**/*": [
      "eslint --fix",
      "prettier --write --ignore-unknown"
    ],
    "!((backend|frontend)/**/*)": "prettier --write"
  }
}
