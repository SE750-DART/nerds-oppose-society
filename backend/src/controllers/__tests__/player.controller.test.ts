describe("Player controller", () => {
  it("Responds with a 201 on successful player creation", () => {
    expect(false).toBe(true);
  });
  it("Responds with a 400 if the game code is invalid", () => {
    expect(false).toBe(true);
  });
});
